version: '3.9'

services:
  olivetti-app:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    image: abhishekjais/olivetti-app:0.5
    container_name: olivetti-app
    ports:
      - "5000:5000"
    restart: unless-stopped